buildName: diting
buildDocker:
  x86_64: auto
  c86_64: auto
  aarch64: auto

index:
  diting-image:
    type: image
    yamlPath:
      x86_64: build/ci/diting/diting.x86_64.yaml
      c86_64: build/ci/diting/diting.x86_64.yaml
      aarch64: build/ci/diting/diting.aarch64.yaml